# scripts/archive — 役目を終えた一回性ツール

現行のシート構成では**動かない/不要**になった移行スクリプトの置き場。参照用に残す（実行しない）。

- `migrate_headers_ja.py` … 2026-06-15 の「英語見出し→日本語化」移行専用。単一 `posts` タブを無条件に開くため、現行のアカウント別タブ構成（`投稿_*` のみ）では WorksheetNotFound で落ちる。全シート日本語化済みのため今後使うことはない。
