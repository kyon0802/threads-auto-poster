# PDCA閉ループ化・第1段 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 分析(Check)と生成(Act)を繋ぐ——勝ち/負け投稿の本文を生成プロンプトに注入し、全生成投稿に型ラベル（フック型/内容型/参照お手本ID）を記録してA/B検証の材料を貯め始める。

**Architecture:** 既存の注入型設計（generate_fn/store/schedule_fn）を維持したまま、(1) collector のER分子に shares を追加、(2) analyzer に負け投稿と28日窓ランキングを追加、(3) sheets にお手本DB/仮説ログのタブ定義と投稿タブ3列を追加（エイリアス層で後方互換）、(4) generator の構造化出力を型ラベル付きオブジェクトに拡張しプロンプトを抽象×具体構成に再編、(5) main_weekly で本文結合を生成前に移動。シート実体の移行はローカルスクリプトで冪等に行う。

**Tech Stack:** Python 3.11 / gspread / Anthropic SDK（構造化出力）/ pytest（モック・API不要）

## Global Constraints

- 公開repoにはコードのみ。実測値・本文資産・シートID・社名はコード/テストに書かない（CLAUDE.md §17b。テストはダミー文言のみ）
- シート列追加は `POSTS_FIELD_ALIASES` エイリアス層経由＝**コード先行デプロイ可**（列が無いシートでは `add_post` が黙って捨てるだけで壊れない）
- 旧 `generate_fn`（文字列リストを返す）との後方互換を維持（既存テスト・手動注入が壊れない）
- 生成プロンプトに渡す具体例は勝ち3本＋負け3本＋お手本7本まで（詰め込みすぎはノイズ＝設計書「抽象×具体」）
- お手本は `状態=retired` を除外して渡す
- 探索枠＝毎回12本中2〜3本（プロンプト指示。実測配分の自動化は第2段）
- テストは既存流儀：`MemoryStore`＋注入・API不要・`python3 -m pytest tests/ -q` 全通過を維持（現在87本）
- コミットは Conventional Commits・日本語本文・`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: collector — shares をエンゲージ率の分子に算入

**Files:**
- Modify: `threads_poster/collector.py:20`
- Test: `tests/test_collect.py`（末尾に追加）

**Interfaces:**
- Consumes: なし
- Produces: `ENGAGE_METRICS = ("likes", "replies", "reposts", "quotes", "shares")`。以後のスナップショットの `engagement_rate` に shares が含まれる（過去スナップショットは再計算しない＝許容済み）

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_collect.py` の末尾に追加（同ファイルの `factory_for`/`acc`/`NOW` をそのまま使う）:

```python
def test_shares_counted_in_engagement():
    """保存/共有(shares)もERの分子に入る（PDCA設計 2026-09-01・保存される投稿を勝ち筋として測るため）。"""
    media = [{"id": "m1", "permalink": "http://x/1", "timestamp": "2026-06-20T10:00:00+0000",
              "media_type": "TEXT", "text": "hello"}]
    insights = {"m1": {"views": 100, "likes": 5, "replies": 3, "reposts": 1, "quotes": 1, "shares": 2}}
    store = MemoryStore(acc(), [])
    Collector(store, client_factory=factory_for(media, insights, {"followers_count": 50}),
              now_fn=lambda: NOW).run()
    # (5+3+1+1+2)/100 = 0.12（shares が入らなければ 0.10 になり失敗する）
    assert store.insights[0]["engagement_rate"] == round(12 / 100, 4), store.insights[0]
```

- [ ] **Step 2: 失敗を確認**

Run: `python3 -m pytest tests/test_collect.py::test_shares_counted_in_engagement -q`
Expected: FAIL（`0.1 == 0.12` の assert 失敗）

- [ ] **Step 3: 実装**

`threads_poster/collector.py:20` を変更:

```python
# エンゲージ率の分子（表示回数で割る）。views は分母なので含めない。
# shares（保存/共有）は 2026-09-01 のPDCA設計で追加：勝敗指標は views だが、
# 「保存される投稿」仮説の検証にERへ算入する。過去スナップショットは再計算しない。
ENGAGE_METRICS = ("likes", "replies", "reposts", "quotes", "shares")
```

- [ ] **Step 4: 全テスト通過を確認**

Run: `python3 -m pytest tests/ -q`
Expected: 88 passed（既存の `test_basic_and_engagement` は shares=0 のため影響なし）

- [ ] **Step 5: Commit**

```bash
git add threads_poster/collector.py tests/test_collect.py
git commit -m "feat: shares（保存/共有）をエンゲージ率の分子に算入

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: analyzer — 負け投稿ランキングと28日窓の勝ち/負けを追加

**Files:**
- Modify: `threads_poster/analyzer.py`（`analyze_insights` 末尾と `analyze_windowed`）
- Test: `tests/test_report_window.py`（末尾に追加）

**Interfaces:**
- Consumes: なし
- Produces: `analyze_insights()` の返り値に `bottom: list[dict]`（views昇順ワースト3・`top` と同じ `_entry` 形式）。`analyze_windowed()` の返り値に `trend_top: list[dict]` / `trend_bottom: list[dict]`（28日窓の勝ち/負け）。Task 5 の `build_prompt` と Task 6 の本文結合がこのキーを読む

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_report_window.py` の末尾に追加（同ファイルの既存ヘルパー/行生成流儀に合わせ、最小の行を直書きする）:

```python
def test_bottom_and_trend_rankings():
    """負けワースト3（views昇順）と、28日窓の trend_top/trend_bottom が出る。"""
    from datetime import datetime
    from threads_poster.analyzer import analyze_insights, analyze_windowed
    def row(pid, dt, views):
        return {"posted_id": pid, "snapshot_date": "2026-09-01",
                "post_datetime": dt, "views": views, "likes": 1,
                "replies": 0, "reposts": 0, "quotes": 0,
                "engagement_rate": 0.1, "text_len": 100}
    rows = [row("p1", "2026-08-30 12:00", 1000),   # 直近7日内・勝ち
            row("p2", "2026-08-29 12:00", 5),      # 直近7日内・負け
            row("p3", "2026-08-10 12:00", 800),    # 7日外・28日内
            row("p4", "2026-08-11 12:00", 2)]      # 7日外・28日内・負け
    a = analyze_insights(rows)
    assert [e["posted_id"] for e in a["bottom"]][:2] == ["p4", "p2"], a["bottom"]
    w = analyze_windowed(rows, now=datetime(2026, 9, 1, 6, 0))
    # trend_* は28日窓＝4本全部が対象。top先頭=p1(1000)、bottom先頭=p4(2)
    assert w["trend_top"][0]["posted_id"] == "p1", w["trend_top"]
    assert w["trend_bottom"][0]["posted_id"] == "p4", w["trend_bottom"]
```

- [ ] **Step 2: 失敗を確認**

Run: `python3 -m pytest tests/test_report_window.py::test_bottom_and_trend_rankings -q`
Expected: FAIL（KeyError: 'bottom'）

- [ ] **Step 3: 実装**

`threads_poster/analyzer.py` の `analyze_insights` 内、`out["top_er"] = ...` の直後（`return out` の前）に追加:

```python
    # 負けランキング（views昇順ワースト3）。勝ち例だけでなく負け例を生成AIに対比で見せる
    # ためのもの（PDCA設計 2026-09-01）。views=0 の投稿こそ負け筆頭なので除外しない。
    bottom = sorted(posts, key=lambda r: _i(r.get("views")))[:3]
    out["bottom"] = [_entry(r) for r in bottom]
```

`analyze_windowed` 内、`out["trend_n_posts"] = trend["n_posts"]` の直後に追加:

```python
    # 生成プロンプト注入用の勝ち/負け実物（28日窓。7日窓では12本しかなくサンプル不足のため）
    out["trend_top"] = trend["top"]
    out["trend_bottom"] = trend["bottom"]
```

- [ ] **Step 4: 全テスト通過を確認**

Run: `python3 -m pytest tests/ -q`
Expected: 89 passed

- [ ] **Step 5: Commit**

```bash
git add threads_poster/analyzer.py tests/test_report_window.py
git commit -m "feat: analyzer に負けワースト3と28日窓の勝ち/負けランキングを追加

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: sheets — 型ラベル3列のエイリアス＋お手本DB/仮説ログの定義＋get_exemplars

**Files:**
- Modify: `threads_poster/sheets.py`（`POSTS_FIELD_ALIASES`・タブ定数群の近く・`Store`・`GoogleSheetStore`・`MemoryStore`）
- Test: `tests/test_phase2.py`（末尾に追加）

**Interfaces:**
- Consumes: 既存 `header_maps` / `_read_tab_raw` / `POSTS_FIELD_ALIASES`
- Produces:
  - `POSTS_FIELD_ALIASES` に `hook_type`（フック型）/ `content_type`（内容型）/ `exemplar_ref`（参照お手本ID）— **既存キーの後ろに追加**（既存タブの列順を崩さない）
  - `EXEMPLAR_TAB_PREFIX = "お手本DB_"` / `HYPOTHESIS_TAB = "仮説ログ"` / `EXEMPLAR_FIELD_ALIASES: dict` / `HYPOTHESIS_HEADER: list[str]`
  - `Store.get_exemplars(account: str) -> list[dict]`（既定実装は `[]`）。内部キー: `exemplar_id/source/text/hook_type/content_type/position/metric/status/retire_reason/note/collected_at`
  - `MemoryStore.get_exemplars` は `self.exemplars: dict[str, list[dict]]`（テストが直接セット）を返す

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_phase2.py` の末尾に追加:

```python
def test_posts_aliases_have_label_columns():
    """型ラベル3列がエイリアス層にあり、新規タブの見出しに含まれる（既存列順は不変）。"""
    from threads_poster.sheets import POSTS_FIELD_ALIASES, per_account_post_headers
    headers = per_account_post_headers()
    assert headers[:3] == ["投稿ID", "投稿日時", "本文"], headers  # 既存先頭は不変
    for col in ("フック型", "内容型", "参照お手本ID"):
        assert col in headers, headers
    assert POSTS_FIELD_ALIASES["hook_type"][0] == "フック型"
    assert POSTS_FIELD_ALIASES["exemplar_ref"][0] == "参照お手本ID"


def test_memorystore_get_exemplars_default_and_set():
    from threads_poster.sheets import MemoryStore
    store = MemoryStore([{"account": "a1"}], [])
    assert store.get_exemplars("a1") == []          # 未設定＝空（生成は従来どおり動く）
    store.exemplars = {"a1": [{"exemplar_id": "ex-001", "text": "お手本本文", "status": "active"}]}
    assert store.get_exemplars("a1")[0]["exemplar_id"] == "ex-001"
```

- [ ] **Step 2: 失敗を確認**

Run: `python3 -m pytest tests/test_phase2.py::test_posts_aliases_have_label_columns tests/test_phase2.py::test_memorystore_get_exemplars_default_and_set -q`
Expected: FAIL（KeyError: 'hook_type' / AttributeError: get_exemplars）

- [ ] **Step 3: 実装**

`threads_poster/sheets.py` の `POSTS_FIELD_ALIASES` の末尾（`"error": [...]` の後）に3キー追加:

```python
    # ★PDCA型ラベル（2026-09-01）。生成AIが自己申告し、A/B検証（型×実測）の紐付けに使う。
    "hook_type":    ["フック型", "hook_type"],
    "content_type": ["内容型", "content_type"],
    "exemplar_ref": ["参照お手本ID", "exemplar_ref"],  # 模倣5連敗ルール（設計書）の判定キー
```

タブ定数群（`KNOWLEDGE_TAB_PREFIX` の直後）に追加:

```python
EXEMPLAR_TAB_PREFIX = "お手本DB_"   # 勝ちパターンDB（自アカ当たり/滑り＋競合。人＋AIが curation）
HYPOTHESIS_TAB = "仮説ログ"          # サイクルごとの仮説→結果（システム追記＋人が手書き可）

EXEMPLAR_FIELD_ALIASES = {
    "exemplar_id":   ["お手本ID", "exemplar_id"],
    "source":        ["出典", "source"],                  # "自アカ" または競合名
    "text":          ["本文", "text"],
    "hook_type":     ["フック型", "hook_type"],
    "content_type":  ["内容型", "content_type"],
    "position":      ["ポジション近接度", "position"],     # 同ポジ/近接/別ポジ
    "metric":        ["実測数", "metric"],                 # 自アカ=views・競合=likes
    "status":        ["状態", "status"],                   # active / retired
    "retire_reason": ["退役理由", "retire_reason"],
    "note":          ["メモ", "note"],
    "collected_at":  ["収集日", "collected_at"],
}
HYPOTHESIS_HEADER = ["日付", "仮説", "検証方法", "結果", "次アクション"]
```

`Store` クラス（`sort_posts_tab` 既定実装の近く）に既定実装を追加:

```python
    def get_exemplars(self, account: str) -> list[dict]:
        """お手本DB（勝ちパターンDB）。既定は空＝DBが無くても生成は従来どおり動く。"""
        return []
```

`GoogleSheetStore` に追加（`get_knowledge` の直後・`_read_tab_raw` は欠落タブで `[]` を返すのでそのまま使える）:

```python
    def get_exemplars(self, account: str) -> list[dict]:
        recs = self._read_tab_raw(f"{EXEMPLAR_TAB_PREFIX}{account}")
        if not recs:
            return []
        to_internal, _ = header_maps(list(recs[0].keys()), EXEMPLAR_FIELD_ALIASES)
        out = [{to_internal.get(k, k): v for k, v in r.items()} for r in recs]
        return [r for r in out if str(r.get("text") or "").strip()]
```

`MemoryStore` に追加（`add_post` の近く）:

```python
    def get_exemplars(self, account: str) -> list[dict]:
        return list(getattr(self, "exemplars", {}).get(account, []))
```

- [ ] **Step 4: 全テスト通過を確認**

Run: `python3 -m pytest tests/ -q`
Expected: 91 passed

- [ ] **Step 5: Commit**

```bash
git add threads_poster/sheets.py tests/test_phase2.py
git commit -m "feat: 型ラベル3列のエイリアス＋お手本DB/仮説ログのスキーマ定義＋get_exemplars

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: generator — 構造化出力を型ラベル付きオブジェクトに拡張し、ラベルをシートへ書く

**Files:**
- Modify: `threads_poster/generator.py`（`Generator.run` / `make_anthropic_generate_fn`）
- Test: `tests/test_phase2.py`（末尾に追加）

**Interfaces:**
- Consumes: Task 3 の `POSTS_FIELD_ALIASES`（hook_type/content_type/exemplar_ref）
- Produces:
  - `_normalize_candidates(candidates: list) -> list[dict]`（各要素 `{"text","hook_type","content_type","exemplar_id"}`。文字列要素も受ける＝後方互換）
  - `Generator.run` は `add_post` に `hook_type`/`content_type`/`exemplar_ref` を渡す。返り値 `kept` は正規化済み dict のリスト（`len()` しか見ていない既存呼び出しは影響なし）
  - `make_anthropic_generate_fn` の JSON スキーマ: `posts` は object 配列（text/hook_type/content_type/exemplar_id 必須・additionalProperties: False）

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_phase2.py` の末尾に追加:

```python
def test_generator_writes_type_labels():
    """dict候補の型ラベルが投稿行に書かれる。文字列候補（旧形式）は空ラベルで通る。"""
    store = MemoryStore([{"account": "a1"}], [])
    store.profiles = {"a1": {"声": "テスト"}}
    store.guideline = [{"分類": "NGワード", "ルール": "絶対", "重大度": "高"}]
    cands = [
        {"text": "月収の手取りを公開する。", "hook_type": "数字提示",
         "content_type": "情報提供", "exemplar_id": "ex-001"},
        "旧形式の文字列候補もそのまま通る。",
    ]
    Generator(store, "a1", generate_fn=lambda p: cands, now_fn=lambda: NOW,
              status="draft").run({}, candidates=cands)
    assert len(store.posts) == 2, store.posts
    assert store.posts[0]["hook_type"] == "数字提示"
    assert store.posts[0]["exemplar_ref"] == "ex-001"
    assert store.posts[1]["hook_type"] == ""   # 旧形式＝空ラベル（後方互換）
```

- [ ] **Step 2: 失敗を確認**

Run: `python3 -m pytest tests/test_phase2.py::test_generator_writes_type_labels -q`
Expected: FAIL（check_post に dict が渡り TypeError、または hook_type が KeyError）

- [ ] **Step 3: 実装**

`threads_poster/generator.py` に module 関数を追加（`build_prompt` の前）:

```python
def _normalize_candidates(candidates: list) -> list[dict]:
    """生成結果を {text, hook_type, content_type, exemplar_id} に正規化する。
    旧形式（文字列のリスト）も受ける（後方互換：既存テスト・手動注入を壊さない）。"""
    out = []
    for c in candidates:
        if isinstance(c, str):
            c = {"text": c}
        out.append({
            "text": str(c.get("text") or ""),
            "hook_type": str(c.get("hook_type") or ""),
            "content_type": str(c.get("content_type") or ""),
            "exemplar_id": str(c.get("exemplar_id") or ""),
        })
    return [c for c in out if c["text"].strip()]
```

`Generator.run` のコンプラゲート〜書込ループを変更（`kept` を dict のリストにする）:

```python
        candidates = _normalize_candidates(candidates)
        now = self.now_fn()
        date = now.strftime("%Y%m%d")
        kept, rejected = [], []
        for c in candidates:
            ok, reasons = check_post(c["text"], ng)
            (kept.append(c) if ok else rejected.append({"text": c["text"], "reasons": reasons}))
```

（`if len(candidates) < self.n_posts:` の警告は正規化の**後**に移す＝空text除去後の実質本数で警告する）

書込ループの `add_post` を変更:

```python
        written = []
        for j, (c, post_dt) in enumerate(zip(kept, schedule), 1):
            row_id = f"{self.id_prefix}-g{date}-{j:02d}"
            self.store.add_post(self.account, {
                "row_id": row_id,
                "post_datetime": post_dt,
                "text": c["text"],
                "media_type": "TEXT",
                "status": self.status,  # draft = 自動公開されない（人が queued に変える）
                "hook_type": c["hook_type"],
                "content_type": c["content_type"],
                "exemplar_ref": c["exemplar_id"],
            })
            written.append(row_id)
```

`schedule_fn` 呼び出しの `len(kept)` はそのまま（dict でも本数は同じ）。

`make_anthropic_generate_fn` のスキーマを変更:

```python
            output_config={"format": {"type": "json_schema", "schema": {
                "type": "object",
                "properties": {"posts": {"type": "array", "items": {
                    "type": "object",
                    "properties": {
                        "text": {"type": "string"},
                        "hook_type": {"type": "string"},
                        "content_type": {"type": "string"},
                        "exemplar_id": {"type": "string"},  # 模倣元お手本ID・無ければ ""
                    },
                    "required": ["text", "hook_type", "content_type", "exemplar_id"],
                    "additionalProperties": False,
                }}},
                "required": ["posts"],
                "additionalProperties": False,
            }}},
```

（max_tokens 見積りの `800 * n` は 1本あたりラベル分で +100 し `900 * n` に更新）

- [ ] **Step 4: 全テスト通過を確認**

Run: `python3 -m pytest tests/ -q`
Expected: 92 passed（`test_generator_pipeline` 等の文字列候補テストは正規化の後方互換で通る）

- [ ] **Step 5: Commit**

```bash
git add threads_poster/generator.py tests/test_phase2.py
git commit -m "feat: 生成出力に型ラベル（フック型/内容型/参照お手本ID）を追加しシートへ記録

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: generator — プロンプトを抽象×具体構成へ再編（勝ち/負け本文＋お手本＋型指示＋探索枠）

**Files:**
- Modify: `threads_poster/generator.py`（`build_prompt` と `Generator.run` の呼び出し部）
- Test: `tests/test_phase2.py`（末尾に追加）

**Interfaces:**
- Consumes: Task 2 の `analysis["trend_top"] / analysis["trend_bottom"]`（各 entry の `text` は Task 6 で結合される。無ければ views のみ）、Task 3 の `store.get_exemplars`
- Produces: `build_prompt(account, profile, guideline, analysis, n, knowledge="", exemplars=None) -> str`。`HOOK_TYPES: list[str]` / `CONTENT_TYPES: list[str]` を module 定数として公開（第2段の配分計算が同じ語彙を使う）

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_phase2.py` の末尾に追加:

```python
def test_build_prompt_injects_examples_and_types():
    """勝ち/負け本文・お手本・型指示・探索枠がプロンプトに入る。retired お手本は除外。"""
    from threads_poster.generator import build_prompt
    analysis = {
        "trend_top": [{"posted_id": "p1", "views": 1200, "text": "勝った本文サンプル"}],
        "trend_bottom": [{"posted_id": "p9", "views": 3, "text": "滑った本文サンプル"}],
    }
    exemplars = [
        {"exemplar_id": "ex-001", "source": "自アカ", "text": "お手本アクティブ",
         "hook_type": "数字提示", "position": "同ポジ", "status": "active"},
        {"exemplar_id": "ex-002", "source": "競合A", "text": "お手本リタイア",
         "hook_type": "逆張り", "position": "別ポジ", "status": "retired"},
    ]
    p = build_prompt("a1", {"声": "テスト"}, [{"分類": "NG", "ルール": "絶対", "重大度": "高"}],
                     analysis, 12, exemplars=exemplars)
    assert "勝った本文サンプル" in p and "1200" in p
    assert "滑った本文サンプル" in p
    assert "ex-001" in p and "お手本アクティブ" in p
    assert "お手本リタイア" not in p          # retired は渡さない
    assert "数字提示" in p and "探索枠" in p   # 型語彙と探索枠の指示
    assert "exemplar_id" in p                  # 模倣元の自己申告指示
```

- [ ] **Step 2: 失敗を確認**

Run: `python3 -m pytest tests/test_phase2.py::test_build_prompt_injects_examples_and_types -q`
Expected: FAIL（TypeError: unexpected keyword 'exemplars'）

- [ ] **Step 3: 実装**

`threads_poster/generator.py` に型語彙を追加（`THREADS_HOOK_RULES` の直前）:

```python
# 型ラベルの語彙（生成の自己申告と第2段の配分計算が共有する。新型は探索枠で自由命名可）
HOOK_TYPES = ["数字提示", "心の声", "逆張り", "失敗談", "問いかけ", "場面描写"]
CONTENT_TYPES = ["情報提供", "共感", "暴露", "求人紹介"]
```

`build_prompt` を差し替え:

```python
def build_prompt(account: str, profile: dict, guideline: list[dict], analysis: dict, n: int,
                 knowledge: str = "", exemplars: list[dict] | None = None) -> str:
    guide = "\n".join(f"- [{g.get('重大度')}] {g.get('分類')}: {g.get('ルール')}" for g in guideline)
    wins = []
    for axis, key in [("時間帯", "by_time"), ("本文長", "by_length"), ("ツリー有無", "by_tree")]:
        cand = [t for t in analysis.get(key, []) if t[1]]
        best = max(cand, key=lambda t: (t[3] if isinstance(t[3], (int, float)) else -1), default=None)
        if best:
            wins.append(f"{axis}は「{best[0]}」が好調")
    if knowledge.strip():
        know_section = ("## 事業ナレッジ（最重要・このアカウントの全知識。声・戦略・合法ライン・"
                        "勝ち筋・フック型・KGI/CVRを含む。これを土台に作る）\n" + knowledge.strip())
    else:
        prof = "\n".join(f"- {k}: {v}" for k, v in profile.items())
        know_section = "## プロフィール\n" + prof

    # ── 実績の実物（勝ち/負けの対比・PDCA閉ループの中核）────────────────────
    def _fmt(entries, label):
        lines = []
        for e in (entries or [])[:3]:
            text = str(e.get("text") or "").strip()
            if text:
                lines.append(f"◆{label}（表示{e.get('views', 0)}回）\n{text}")
        return lines
    real = _fmt(analysis.get("trend_top"), "勝ち") + _fmt(analysis.get("trend_bottom"), "負け")
    real_section = ("## 実績の実物（直近28日・勝ちと負けの差から学ぶ。負けの真似は禁止）\n"
                    + "\n\n".join(real) + "\n\n") if real else ""

    # ── お手本DB（active のみ・最大7本・別ポジは構造だけ参考）─────────────────
    ex_lines = []
    for ex in (exemplars or []):
        if str(ex.get("status") or "active").strip() == "retired":
            continue
        caution = ("※別ポジの参考：フックと構造のみ真似る。声・主張・内容はコピーしない"
                   if str(ex.get("position") or "") == "別ポジ" else "")
        ex_lines.append(f"[{ex.get('exemplar_id')}]（出典:{ex.get('source')}／フック型:{ex.get('hook_type')}）{caution}\n"
                        f"{str(ex.get('text') or '').strip()}")
        if len(ex_lines) >= 7:
            break
    ex_section = ("## お手本DB（模倣した手本のIDを exemplar_id で必ず申告する）\n"
                  + "\n\n".join(ex_lines) + "\n\n") if ex_lines else ""

    type_section = (
        "## 型ラベルと探索枠（全投稿に必ず自己申告）\n"
        f"- フック型: {' / '.join(HOOK_TYPES)}（探索枠では新しい型を短い名前で自由命名可）\n"
        f"- 内容型: {' / '.join(CONTENT_TYPES)}\n"
        f"- {n}本のうち2〜3本は**探索枠**＝上の実績に無い新しいフック・切り口を意図的に試す\n"
        "- 各投稿の出力は text / hook_type / content_type / exemplar_id（模倣元。無ければ空文字）\n"
    )

    return (
        f"あなたはThreadsアカウント「{account}」の専属コンテンツ戦略担当です。\n"
        f"下記の事業ナレッジとガイドライン（規約・法令・NG）を**厳守**し、\n"
        f"実績の勝ちパターンを踏まえて、エンゲージ（最終的には送客/成立=CVR）が伸びる"
        f"翌週の投稿案を{n}本作成してください。\n\n"
        f"{know_section}\n\n"
        f"## ガイドライン（厳守・違反した投稿は機械的に破棄される）\n{guide}\n\n"
        f"## 実績の勝ちパターン（集計）\n" + ("／".join(wins) if wins else "（データ蓄積中・お手本の型を踏襲）") + "\n\n"
        f"{real_section}"
        f"{ex_section}"
        f"{type_section}\n"
        f"{THREADS_HOOK_RULES}\n\n"
        f"## 出力要件\n"
        f"- 各投稿は独立した完成本文（そのまま投稿できる形）\n"
        f"- NGワードは絶対に使わない／本文に外部URLを書かない（誘導はプロフィール動線）\n"
        f"- ナレッジの声と勝ち筋・フック型を踏襲\n"
    )
```

`Generator.run` の `build_prompt` 呼び出しを変更（お手本DBを読む）:

```python
        if candidates is None:
            exemplars = self.store.get_exemplars(self.account)
            prompt = build_prompt(self.account, profile, guideline, analysis, self.n_posts,
                                  knowledge=knowledge, exemplars=exemplars)
            candidates = self.generate_fn(prompt)
```

- [ ] **Step 4: 全テスト通過を確認**

Run: `python3 -m pytest tests/ -q`
Expected: 93 passed

- [ ] **Step 5: Commit**

```bash
git add threads_poster/generator.py tests/test_phase2.py
git commit -m "feat: 生成プロンプトを抽象×具体構成に再編（勝ち/負け本文＋お手本DB＋型指示＋探索枠）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: main_weekly — 本文結合を生成前へ移動（勝ち/負け本文を生成に届かせる）

**Files:**
- Modify: `main_weekly.py`（`enrich_tops_with_text` と Generator 呼び出し前後）
- Test: `tests/test_report_window.py`（末尾に追加）

**Interfaces:**
- Consumes: Task 2 の `trend_top`/`trend_bottom` キー、既存 `posts_all`（get_posts の結果）
- Produces: `enrich_tops_with_text(posts, account, analysis)` が `("top", "top_er", "trend_top", "trend_bottom")` の4キーに本文を結合する。呼び出し位置は `analysis = Analyzer(...).run(acc)` の直後（生成より前・全事業で実行）

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_report_window.py` の末尾に追加:

```python
def test_enrich_covers_trend_keys():
    """trend_top / trend_bottom にも本文が結合される（生成プロンプト注入の材料）。"""
    from main_weekly import enrich_tops_with_text
    posts = [{"account": "a1", "posted_id": "p1", "text": "勝ち本文"},
             {"account": "a1", "posted_id": "p9", "text": "負け本文"}]
    analysis = {"top": [{"posted_id": "p1"}], "top_er": [],
                "trend_top": [{"posted_id": "p1"}], "trend_bottom": [{"posted_id": "p9"}]}
    enrich_tops_with_text(posts, "a1", analysis)
    assert analysis["trend_top"][0]["text"] == "勝ち本文"
    assert analysis["trend_bottom"][0]["text"] == "負け本文"
```

- [ ] **Step 2: 失敗を確認**

Run: `python3 -m pytest tests/test_report_window.py::test_enrich_covers_trend_keys -q`
Expected: FAIL（KeyError: 'text'）

- [ ] **Step 3: 実装**

`main_weekly.py:110` のキータプルを変更:

```python
    for key in ("top", "top_er", "trend_top", "trend_bottom"):
        for r in analysis.get(key, []):
            r["text"] = pid2text.get(str(r.get("posted_id") or ""), "")
```

`main_weekly.py` のアカウントループで、`analysis = Analyzer(store, now_fn=lambda: now_local).run(acc)` の**直後**に追加し、`if in_email:` ブロック内の `enrich_tops_with_text(posts_all, acc, analysis)` は**削除**する:

```python
                analysis = Analyzer(store, now_fn=lambda: now_local).run(acc)
                # ランキングに本文を結合（★生成より前・全事業で実施。勝ち/負けの実物を
                # 生成プロンプトへ届かせる＝PDCA閉ループの結線・2026-09-01設計）
                enrich_tops_with_text(posts_all, acc, analysis)
```

- [ ] **Step 4: 全テスト通過を確認**

Run: `python3 -m pytest tests/ -q`
Expected: 94 passed

- [ ] **Step 5: Commit**

```bash
git add main_weekly.py tests/test_report_window.py
git commit -m "feat: 勝ち/負け本文の結合を生成前に移動（PDCA閉ループの結線）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: シート移行スクリプト（3列追加＋お手本DB/仮説ログタブ作成・冪等）

**Files:**
- Create: `scripts/add_pdca_columns.py`

**Interfaces:**
- Consumes: Task 3 の `EXEMPLAR_TAB_PREFIX` / `EXEMPLAR_FIELD_ALIASES` / `HYPOTHESIS_TAB` / `HYPOTHESIS_HEADER` / `POSTS_TAB_PREFIX` / `with_retry`
- Produces: 人がローカルで実行する移行ツール。シートIDは**引数で渡す**（§17b: コードに書かない）。既定 DRY-RUN・`--apply` で書込・冪等（再実行しても二重列/二重タブを作らない）

- [ ] **Step 1: スクリプトを書く**（scripts/ は人が実行する運用ツール層＝既存流儀どおりユニットテスト対象外。DRY-RUN が検証手段）

```python
"""投稿タブへPDCA型ラベル3列を追加し、お手本DB_<acc>・仮説ログ タブを作成する（冪等）。

使い方（シートIDは§17bによりコードに書かない・引数で渡す）:
  DRY-RUN:  python3 scripts/add_pdca_columns.py --sheet-id <ID> [--sheet-id <ID2> ...]
  実書込:   python3 scripts/add_pdca_columns.py --sheet-id <ID> --apply

認証: .env の GOOGLE_SERVICE_ACCOUNT_FILE（scripts/local_run.sh と同じ流儀）
"""
import argparse
import json
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from threads_poster.sheets import (  # noqa: E402
    EXEMPLAR_FIELD_ALIASES, EXEMPLAR_TAB_PREFIX, HYPOTHESIS_HEADER, HYPOTHESIS_TAB,
    POSTS_TAB_PREFIX, with_retry,
)

NEW_POST_COLUMNS = ["フック型", "内容型", "参照お手本ID"]
EXEMPLAR_HEADER = [names[0] for names in EXEMPLAR_FIELD_ALIASES.values()]


def open_sheet(sheet_id: str):
    import gspread
    from google.oauth2.service_account import Credentials
    sa_file = os.environ.get("GOOGLE_SERVICE_ACCOUNT_FILE")
    sa_json = os.environ.get("GOOGLE_SERVICE_ACCOUNT_JSON")
    if sa_file:
        info = json.load(open(sa_file))
    elif sa_json:
        info = json.loads(sa_json)
    else:
        raise SystemExit("GOOGLE_SERVICE_ACCOUNT_FILE か GOOGLE_SERVICE_ACCOUNT_JSON が必要です")
    creds = Credentials.from_service_account_info(
        info, scopes=["https://www.googleapis.com/auth/spreadsheets"])
    return with_retry(lambda: gspread.authorize(creds).open_by_key(sheet_id))


def migrate(sheet_id: str, apply: bool) -> None:
    sh = open_sheet(sheet_id)
    tabs = {w.title: w for w in with_retry(sh.worksheets)}
    accounts = [t[len(POSTS_TAB_PREFIX):] for t in tabs if t.startswith(POSTS_TAB_PREFIX)]
    print(f"--- sheet={sheet_id[:8]}… 投稿タブ={len(accounts)}件 ---")

    for acc in accounts:
        ws = tabs[f"{POSTS_TAB_PREFIX}{acc}"]
        header = with_retry(lambda: ws.row_values(1))
        missing = [c for c in NEW_POST_COLUMNS if c not in header]
        if not missing:
            print(f"  投稿_{acc}: 3列あり（スキップ）")
        else:
            print(f"  投稿_{acc}: 追加 {missing}" + ("" if apply else "（DRY-RUN）"))
            if apply:
                # 見出し行の右端に不足列だけ追記（既存列は動かさない＝データ非破壊）
                with_retry(lambda: ws.update(
                    [[*missing]],
                    range_name=f"{gspread_col(len(header) + 1)}1",
                    value_input_option="RAW"))

    for acc in accounts:
        title = f"{EXEMPLAR_TAB_PREFIX}{acc}"
        if title in tabs:
            print(f"  {title}: あり（スキップ）")
        else:
            print(f"  {title}: 作成" + ("" if apply else "（DRY-RUN）"))
            if apply:
                ws = with_retry(lambda: sh.add_worksheet(title, rows=200, cols=len(EXEMPLAR_HEADER)))
                with_retry(lambda: ws.append_row(EXEMPLAR_HEADER, value_input_option="RAW"))

    if HYPOTHESIS_TAB in tabs:
        print(f"  {HYPOTHESIS_TAB}: あり（スキップ）")
    else:
        print(f"  {HYPOTHESIS_TAB}: 作成" + ("" if apply else "（DRY-RUN）"))
        if apply:
            ws = with_retry(lambda: sh.add_worksheet(HYPOTHESIS_TAB, rows=500, cols=len(HYPOTHESIS_HEADER)))
            with_retry(lambda: ws.append_row(HYPOTHESIS_HEADER, value_input_option="RAW"))


def gspread_col(n: int) -> str:
    """1始まりの列番号→A1表記の列文字（1→A, 27→AA）。"""
    s = ""
    while n:
        n, r = divmod(n - 1, 26)
        s = chr(65 + r) + s
    return s


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--sheet-id", action="append", required=True,
                    help="対象スプレッドシートID（複数指定可・公開repoに書かないこと）")
    ap.add_argument("--apply", action="store_true", help="実際に書き込む（既定はDRY-RUN）")
    args = ap.parse_args()
    for sid in args.sheet_id:
        migrate(sid, args.apply)
    print("完了" + ("" if args.apply else "（DRY-RUN・書込なし。--apply で実行）"))
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 文法・DRY-RUN経路の検証**

Run: `python3 -c "import ast; ast.parse(open('scripts/add_pdca_columns.py').read())" && python3 scripts/add_pdca_columns.py --help`
Expected: ヘルプが表示される（引数仕様が読める）

- [ ] **Step 3: 全テスト通過を確認**（既存スイートに影響が無いこと）

Run: `python3 -m pytest tests/ -q`
Expected: 94 passed

- [ ] **Step 4: Commit**

```bash
git add scripts/add_pdca_columns.py
git commit -m "feat: PDCA用シート移行スクリプト（3列追加＋お手本DB/仮説ログ作成・冪等・DRY-RUN既定）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: ドキュメント同期（CLAUDE.md・CHANGELOG §29）

**Files:**
- Modify: `CLAUDE.md`（§4 ファイル構成・§5 スキーマ）
- Modify: `docs/CHANGELOG.md`（§29 追記）

**Interfaces:**
- Consumes: Task 1〜7 の成果
- Produces: 引き継ぎドキュメントの実態同期（改修ログは CHANGELOG に、現在仕様は CLAUDE.md に＝リポジトリ規約）

- [ ] **Step 1: CLAUDE.md を更新**

§5（スキーマ）の投稿タブ見出しの行に3列を追記:

```
`投稿ID`(row_id) / `投稿日時`(post_datetime, JST `YYYY-MM-DD HH:MM`・文字列書式) / `本文`(text) / `メディア種類`(media_type, TEXT/IMAGE/VIDEO/CAROUSEL) / `メディアURL`(media_url) / `返信先ID`(reply_to=親の投稿ID) / `返信できる人`(reply_control) / `状態`(status) / `投稿後ID`(posted_id) / `投稿実施日時`(posted_at) / `エラー`(error) / `フック型`(hook_type) / `内容型`(content_type) / `参照お手本ID`(exemplar_ref)
```

同じく§5「人が curation するタブ」の直後に追記:

```
- `お手本DB_<acc>`（お手本ID/出典/本文/フック型/内容型/ポジション近接度/実測数/状態/退役理由/メモ/収集日） … 勝ちパターンDB。自アカ当たり/滑り＋競合の当たり投稿。生成は `状態=active` のみ参照。設計は docs/superpowers/specs/2026-09-01-pdca-closed-loop-design.md
- `仮説ログ`（日付/仮説/検証方法/結果/次アクション） … サイクルごとのPDCA記録（第1段は人が記入・第2段で自動追記）
```

§4 の `scripts/` 一覧に1行追記:

```
  add_pdca_columns.py         PDCA移行: 投稿タブ3列追加＋お手本DB/仮説ログ作成（冪等・DRY-RUN既定）
```

- [ ] **Step 2: CHANGELOG に §29 を追記**

`docs/CHANGELOG.md` 末尾に追加:

```markdown
---

## §29 改修ログ（2026-09-01）PDCA閉ループ化・第1段

設計の正本: docs/superpowers/specs/2026-09-01-pdca-closed-loop-design.md

**問題**: 分析(Check)と生成(Act)が繋がっていない開ループだった。generator が分析から
使うのは時間帯/本文長/ツリー有無の3集計値のみで、当たり投稿の本文は週次メールで
人に見せて終わり。型を記録する列も無く「どの型が勝ったか」の紐付けが原理的に不可能。

**実装（第1段）**:
- shares（保存/共有）をERの分子に算入（collector.ENGAGE_METRICS）
- analyzer に bottom（負けワースト3）と trend_top/trend_bottom（28日窓）を追加
- 投稿タブに3列追加: フック型/内容型/参照お手本ID（エイリアス層で後方互換＝
  列が無いシートでも壊れない。実体は scripts/add_pdca_columns.py で移行）
- お手本DB_<acc>・仮説ログ タブを新設（get_exemplars / EXEMPLAR_FIELD_ALIASES）
- 生成の構造化出力を {text, hook_type, content_type, exemplar_id} に拡張し、
  型ラベルをシートに記録（旧形式=文字列リストも後方互換で受ける）
- プロンプトを抽象×具体構成に再編: 勝ち3本＋負け3本の実物本文（対比学習）＋
  お手本DB（activeのみ最大7本・別ポジは構造のみ参考）＋型語彙＋探索枠2〜3本
- 本文結合（enrich_tops_with_text）を生成前へ移動し trend_top/trend_bottom もカバー

**運用**: 競合お手本の収集は半自動 — スクショを 製造業Threads/03_お手本inbox・
占いThreads-note事業/05_お手本inbox（ローカル・repo外）へ投下→セッションで
「inbox処理して」→ AIがタグ付けしてお手本DBタブへ登録。

**第2段（未実装）**: 型別中央値→配分の自動計算・模倣5連敗の自動退役・
改行数帯 by_linebreaks・仮説ログ自動追記・時間帯窓の実測改訂。
```

- [ ] **Step 3: 全テスト＋§17b最終確認**

Run: `python3 -m pytest tests/ -q && git diff --cached --stat`
`git diff` 上に実シートID・実社名・実メールアドレスが無いことを目視確認。
Expected: 94 passed・機密なし

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md docs/CHANGELOG.md
git commit -m "docs: PDCA第1段の仕様反映（スキーマ3列＋お手本DB/仮説ログ＋CHANGELOG §29）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## 実装後の手動ステップ（コード外・オーナーと実施）

1. **シート移行の実行**: `python3 scripts/add_pdca_columns.py --sheet-id <各事業のID>` でDRY-RUN確認→`--apply`（IDは非公開メモから。4事業分）
2. **プロフィール拡充 curation**（対話作業）: 各 `プロフィール_<acc>` に Persona（何者か/使命/価値観/ストーリー）・Voice（口調/改行/テンポ）・判断基準（何を言わないか）の行を追加。設計書 §人格化 参照
3. **お手本の初期投入**: 各アカの過去当たり投稿（takumi/pashi は実績あり）を `お手本DB_<acc>` に登録＋inboxのスクショ処理
4. push 後、次サイクル（09-02 06:00 JST）の weekly.yml で新プロンプトが稼働。ログの `生成完了: 候補12 / 合格…` と投稿タブの型ラベル列を確認
